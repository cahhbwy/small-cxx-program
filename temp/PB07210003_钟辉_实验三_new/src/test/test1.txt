	/*
	 *

	Table t("tt");
	ColInfo i[2];
	i[0].Set( "ca", INTEGER_TYPE );
	i[1].Set( "cb", STRING_TYPE, 10 );

	TableInfo in(i, 2);
	t.Create( in );
	t.Write();

	Print p;
	p.print( t.Show() );

	Table t2("tt");
	t2.Read();
	p.print( t2.Show() );

	cout << "sss" << endl;

	 */
